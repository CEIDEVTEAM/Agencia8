import React from 'react';
import PageIndicator from './PageIndicator';
import routes from '../routes';


const PageIndicatorContainer = () => {
  return (
    <PageIndicator routes={routes} />
  );
};

export default PageIndicatorContainer;
